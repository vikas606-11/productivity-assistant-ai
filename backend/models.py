from database import db

# Placeholder for database models.
# Tables will be defined here in future commits.
# Example:
# class User(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     name = db.Column(db.String(80), nullable=False)
